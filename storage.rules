rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read;
    }
    match /users/{uid=**}{
    	allow write: if request.auth != null;
    }
  }
}